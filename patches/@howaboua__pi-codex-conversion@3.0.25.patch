diff --git a/dist/adapter/activation/config-store.js b/dist/adapter/activation/config-store.js
index 3295a3aa7be1a33f8ac5d0bc78b489fa16df9ddf..914c9d1d5351dc9538e6645c364d77801d596478 100644
--- a/dist/adapter/activation/config-store.js
+++ b/dist/adapter/activation/config-store.js
@@ -142,12 +142,19 @@ export function hasFolderCodexConversionConfig(cwd, projectTrusted) {
 function applyProcessOverrides(config, env) {
     const cacheEnvironment = readCodexCacheEnvironment(env);
     const fast = env["PI_CODEX_FAST"]?.trim().toLowerCase();
+    const cacheKeepalive = env["PI_CODEX_CACHE_KEEPALIVE"]?.trim().toLowerCase();
+    const cacheKeepaliveOverride = cacheKeepalive === "1" || cacheKeepalive === "true"
+        ? true
+        : cacheKeepalive === "0" || cacheKeepalive === "false"
+            ? false
+            : undefined;
     const fastOverride = fast === "1" || fast === "true"
         ? true
         : fast === "0" || fast === "false"
             ? false
             : undefined;
     if (fastOverride === undefined
+        && cacheKeepaliveOverride === undefined
         && cacheEnvironment.diagnostics === undefined)
         return config;
     return {
@@ -155,6 +162,7 @@ function applyProcessOverrides(config, env) {
         openai: {
             ...config.openai,
             ...(fastOverride !== undefined ? { fast: fastOverride } : {}),
+            ...(cacheKeepaliveOverride !== undefined ? { cacheKeepalive: cacheKeepaliveOverride } : {}),
             ...(cacheEnvironment.diagnostics !== undefined
                 ? { cacheDiagnostics: cacheEnvironment.diagnostics }
                 : {}),
diff --git a/dist/tools/native/binary.js b/dist/tools/native/binary.js
index 403b08f7741f82a1529fcfd6433c29d16e72ec92..9740468a52fb4a976f04e0a8ffd43474cf15d093 100644
--- a/dist/tools/native/binary.js
+++ b/dist/tools/native/binary.js
@@ -22,6 +22,7 @@ export function getBundledToolBinaryPath(toolName, target = {}, customDir) {
         if (existsSync(customBinary))
             return customBinary;
     }
-    const binary = join(packageRoot(), "src", "tools", toolDir, "bin", `${platform}-${arch}`, exe);
+    const root = packageRoot().replace(/([\\/]app\.asar)(?=[\\/])/, "$1.unpacked");
+    const binary = join(root, "src", "tools", toolDir, "bin", `${platform}-${arch}`, exe);
     return existsSync(binary) ? binary : undefined;
 }
diff --git a/src/adapter/activation/config-store.ts b/src/adapter/activation/config-store.ts
index 2c2e7859d38509a042b7d541ba1deaad83218c10..acd768ae1c3bfede8c6accf1f1ba4f11903ae809 100644
--- a/src/adapter/activation/config-store.ts
+++ b/src/adapter/activation/config-store.ts
@@ -149,6 +149,12 @@ export function hasFolderCodexConversionConfig(cwd: string, projectTrusted: bool
 function applyProcessOverrides(config: CodexConversionConfig, env: NodeJS.ProcessEnv): CodexConversionConfig {
 	const cacheEnvironment = readCodexCacheEnvironment(env);
 	const fast = env["PI_CODEX_FAST"]?.trim().toLowerCase();
+	const cacheKeepalive = env["PI_CODEX_CACHE_KEEPALIVE"]?.trim().toLowerCase();
+	const cacheKeepaliveOverride = cacheKeepalive === "1" || cacheKeepalive === "true"
+		? true
+		: cacheKeepalive === "0" || cacheKeepalive === "false"
+			? false
+			: undefined;
 	const fastOverride = fast === "1" || fast === "true"
 		? true
 		: fast === "0" || fast === "false"
@@ -156,6 +162,7 @@ function applyProcessOverrides(config: CodexConversionConfig, env: NodeJS.Proces
 			: undefined;
 	if (
 		fastOverride === undefined
+		&& cacheKeepaliveOverride === undefined
 		&& cacheEnvironment.diagnostics === undefined
 	) return config;
 	return {
@@ -163,6 +170,7 @@ function applyProcessOverrides(config: CodexConversionConfig, env: NodeJS.Proces
 		openai: {
 			...config.openai,
 			...(fastOverride !== undefined ? { fast: fastOverride } : {}),
+			...(cacheKeepaliveOverride !== undefined ? { cacheKeepalive: cacheKeepaliveOverride } : {}),
 			...(cacheEnvironment.diagnostics !== undefined
 				? { cacheDiagnostics: cacheEnvironment.diagnostics }
 				: {}),
diff --git a/src/tools/native/binary.ts b/src/tools/native/binary.ts
index aa7be274e49e1d38ee2f4db325bcde26e0dff0b2..dc6f0edd52f36b9ddc60521bdc830bf2c5a79848 100644
--- a/src/tools/native/binary.ts
+++ b/src/tools/native/binary.ts
@@ -27 +27,2 @@ export function getBundledToolBinaryPath(toolName: string, target: { platform?:
-	const binary = join(packageRoot(), "src", "tools", toolDir, "bin", `${platform}-${arch}`, exe);
+	const root = packageRoot().replace(/([\\/]app\.asar)(?=[\\/])/, "$1.unpacked");
+	const binary = join(root, "src", "tools", toolDir, "bin", `${platform}-${arch}`, exe);
diff --git a/dist/providers/openai-codex/responses-lite-model.js b/dist/providers/openai-codex/responses-lite-model.js
--- a/dist/providers/openai-codex/responses-lite-model.js
+++ b/dist/providers/openai-codex/responses-lite-model.js
@@ -6 +6 @@
-    return /^(?:gpt-5\.6-(?:luna|terra|sol)|gpt-daybreak-(?:blue|red)-latest)$/.test(id.toLowerCase());
+    return /^(?:gpt-6-astra|gpt-5\.6-(?:luna|terra|sol)|gpt-daybreak-(?:blue|red)-latest)$/.test(id.toLowerCase());
diff --git a/src/providers/openai-codex/responses-lite-model.ts b/src/providers/openai-codex/responses-lite-model.ts
--- a/src/providers/openai-codex/responses-lite-model.ts
+++ b/src/providers/openai-codex/responses-lite-model.ts
@@ -7 +7 @@
-	return /^(?:gpt-5\.6-(?:luna|terra|sol)|gpt-daybreak-(?:blue|red)-latest)$/.test(id.toLowerCase());
+	return /^(?:gpt-6-astra|gpt-5\.6-(?:luna|terra|sol)|gpt-daybreak-(?:blue|red)-latest)$/.test(id.toLowerCase());
diff --git a/dist/adapter/activation/cache-keepalive.js b/dist/adapter/activation/cache-keepalive.js
--- a/dist/adapter/activation/cache-keepalive.js
+++ b/dist/adapter/activation/cache-keepalive.js
@@ -13 +13 @@
-    if ((modelId === "gpt-5.6-sol" || modelId === "gpt-5.6-terra") && config.cacheKeepalive) {
+    if ((modelId === "gpt-6-astra" || modelId === "gpt-5.6-sol" || modelId === "gpt-5.6-terra") && config.cacheKeepalive) {
diff --git a/src/adapter/activation/cache-keepalive.ts b/src/adapter/activation/cache-keepalive.ts
--- a/src/adapter/activation/cache-keepalive.ts
+++ b/src/adapter/activation/cache-keepalive.ts
@@ -26 +26 @@
-	if ((modelId === "gpt-5.6-sol" || modelId === "gpt-5.6-terra") && config.cacheKeepalive) {
+	if ((modelId === "gpt-6-astra" || modelId === "gpt-5.6-sol" || modelId === "gpt-5.6-terra") && config.cacheKeepalive) {
