diff --git a/build.zig b/build.zig
index 68900d5..3a1051f 100644
--- a/build.zig
+++ b/build.zig
@@ -79,6 +79,7 @@ pub fn build(b: *std.Build) void {

     const exe_tests = b.addTest(.{
         .root_module = exe.root_module,
+        .filters = if (b.option([]const u8, "test-filter", "Run matching tests only")) |filter| &.{filter} else &.{},
     });
     const run_exe_tests = b.addRunArtifact(exe_tests);
     run_exe_tests.step.dependOn(b.getInstallStep());
diff --git a/src/acp/prompt.zig b/src/acp/prompt.zig
index 69204d9..ecea68a 100644
--- a/src/acp/prompt.zig
+++ b/src/acp/prompt.zig
@@ -964,17 +964,17 @@ fn buildAgentConfig(
     current_prompt_is_external: bool,
 ) agent_runtime.Config {
     return .{
-        .system_prompt = state.cfg.prompt_policy.system_prompt,
+        .system_prompt = if (state.cfg.allow_host_tools) "" else state.cfg.prompt_policy.system_prompt,
         .host_instructions = sections.host_instructions,
-        .model_prompt_overlay = state.cfg.prompt_policy.modelPromptOverlay(session.model),
-        .skill_catalog = sections.skill_catalog,
+        .model_prompt_overlay = if (state.cfg.allow_host_tools) null else state.cfg.prompt_policy.modelPromptOverlay(session.model),
+        .skill_catalog = if (state.cfg.allow_host_tools) .{ .skills = &.{} } else sections.skill_catalog,
         .gateway_retry_count = state.cfg.gateway_retry_count,
         .gateway_chat_url = state.cfg.gateway_chat_url,
         .advertised_tool_names = sections.advertised_tool_names,
         .advertised_functions = sections.advertised_functions,
         .initial_dynamic_tools = state.host_tools.dynamic_tools,
         .provider_capabilities = state.cfg.provider_set.select(session.provider).capabilities,
-        .custom_tool_guidance = sections.custom_tool_guidance,
+        .custom_tool_guidance = if (state.cfg.allow_host_tools) "" else sections.custom_tool_guidance,
         .agent_step_limit = session.agent_step_limit,
         .max_tool_result_bytes = session.max_tool_result_bytes,
         .cancel_flag = &session.cancel_flag,
@@ -1000,7 +1000,7 @@ fn buildAgentConfig(
                 current_prompt_is_external
         else
             false,
-        .enforce_response_language = !state.cfg.minimal_kernel,
+        .enforce_response_language = !(state.cfg.minimal_kernel or state.cfg.allow_host_tools),
         .context_limits = state.context_limits,
     };
 }
@@ -1452,6 +1452,7 @@ fn finalizeTurn(raw_ctx: *anyopaque, turn_id: u64, outcome: types.TurnPresentati

 fn appendRuntimeContext(raw_ctx: *anyopaque, arena: Allocator, messages: *std.ArrayList(ChatMessage)) !void {
     const ctx: *AcpContext = @ptrCast(@alignCast(raw_ctx));
+    if (ctx.state.cfg.allow_host_tools) return;
     const session = if (ctx.state.active_session) |*active| active else unreachable;
     try ctx.state.cfg.context_registry.appendDefaultTransient(.{
         .workspace_root = ctx.state.workspace_root,
@@ -1464,6 +1465,7 @@ fn appendRuntimeContext(raw_ctx: *anyopaque, arena: Allocator, messages: *std.Ar

 fn appendStaticContext(raw_ctx: *anyopaque, arena: Allocator, messages: *std.ArrayList(ChatMessage)) !void {
     const ctx: *AcpContext = @ptrCast(@alignCast(raw_ctx));
+    if (ctx.state.cfg.allow_host_tools) return;
     try ctx.state.cfg.context_registry.appendDefaultStatic(.{
         .project_context = ctx.modelVisibleProjectContext(),
     }, arena, messages);
diff --git a/src/acp/server.zig b/src/acp/server.zig
index aef731e..feed750 100644
--- a/src/acp/server.zig
+++ b/src/acp/server.zig
@@ -1734,7 +1734,7 @@ fn handleInitialize(state: *ServerState, alloc: Allocator, msg: *jsonrpc.Message
     var request = parseInitializeRequest(
         alloc,
         msg.params_raw,
-        state.cfg.minimal_kernel,
+        state.cfg.minimal_kernel or state.cfg.allow_host_tools,
     ) catch {
         return state.writer.writeError(alloc, msg.id, .{
             .code = ErrorCode.invalid_params,
@@ -1914,6 +1914,7 @@ fn handleInitialize(state: *ServerState, alloc: Allocator, msg: *jsonrpc.Message
     if (state.host_instructions.len > 0) alloc.free(state.host_instructions);
     state.host_instructions = request.host_instructions;
     request.host_instructions = &.{};
+    if (state.cfg.allow_host_tools) state.context_enabled = false;
     debug_trace.logf("acp", "libfx host capabilities tools={d} instructions_bytes={d}", .{
         state.host_tools.tools.len,
         state.host_instructions.len,
diff --git a/src/core/agent/runtime/orchestrator.zig b/src/core/agent/runtime/orchestrator.zig
index 7b7afa9..f4fa901 100644
--- a/src/core/agent/runtime/orchestrator.zig
+++ b/src/core/agent/runtime/orchestrator.zig
@@ -5622,6 +5622,7 @@ fn processQueuedPromptLoop(
                 }
             }
             const request_data = agent_stream_provider.RequestData{
+                .session_id = lifecycle.scope.session_id,
                 .model = gateway_model,
                 .instructions = gateway_instructions.items,
                 .messages = request_messages,
diff --git a/src/core/agent/stream_provider.zig b/src/core/agent/stream_provider.zig
index d5b61c5..6f22240 100644
--- a/src/core/agent/stream_provider.zig
+++ b/src/core/agent/stream_provider.zig
@@ -161,6 +161,7 @@ test "host-managed credential lease exposes no secret or account metadata" {
 /// Pure provider input used by request serializers and permission reviewers.
 /// Every slice and JSON value is borrowed for the call.
 pub const RequestData = struct {
+    session_id: ?[]const u8 = null,
     model: []const u8,
     instructions: []const types.ChatMessage = &.{},
     messages: []const types.ChatMessage,
@@ -237,6 +238,7 @@ pub const ModelRequest = struct {

     pub fn data(self: ModelRequest) RequestData {
         return .{
+            .session_id = self.session_id,
             .model = self.model,
             .instructions = self.instructions,
             .messages = self.messages,
diff --git a/src/core/cli/acp_runner.zig b/src/core/cli/acp_runner.zig
index 3949ab4..2e9912e 100644
--- a/src/core/cli/acp_runner.zig
+++ b/src/core/cli/acp_runner.zig
@@ -43,6 +43,7 @@ pub const Config = struct {
     saved_directories_suppressed: bool = false,
     allow_acp_mcp: bool = true,
     allow_native_tools: bool = true,
+    allow_host_tools: bool = false,
     minimal_kernel: bool = false,
 };

diff --git a/src/gateway/openai_codex.zig b/src/gateway/openai_codex.zig
index e048e95..dd15efd 100644
--- a/src/gateway/openai_codex.zig
+++ b/src/gateway/openai_codex.zig
@@ -70,6 +70,12 @@ pub fn buildRequest(
     const writer = &out.writer;
     try writer.writeAll("{\"model\":");
     try std.json.Stringify.value(request.model, .{}, writer);
+    const cache_key = io_mod.getenv("FX_T3_PROMPT_CACHE_KEY") orelse request.session_id;
+    if (cache_key) |session_id| if (session_id.len > 0) {
+        if (session_id.len > 64) return error.InvalidPromptCacheKey;
+        try writer.writeAll(",\"prompt_cache_key\":");
+        try std.json.Stringify.value(session_id, .{}, writer);
+    };
     try writer.writeAll(",\"store\":false,\"stream\":true,\"instructions\":");
     try std.json.Stringify.value(instructions.written(), .{}, writer);
     try writer.writeAll(",\"input\":[");
@@ -263,7 +269,12 @@ pub fn streamPrepared(
     if (request.cancel_flag.load(.seq_cst)) return stream_provider.failResult(error.Cancelled);
     var auth_headers = try requestAuthHeaders(alloc, request.credential);
     defer auth_headers.deinit(alloc);
-    const request_endpoint = if (io_mod.getenv(e2e_endpoint_env)) |override| endpoint: {
+    const proxy_endpoint: ?[]u8 = if (io_mod.getenv("FX_T3_CODEX_PROXY_URL")) |base| proxy: {
+        if (!gateway_client.isLoopbackHttpUrl(base)) return stream_provider.failResult(error.InvalidCodexProxy);
+        break :proxy try std.fmt.allocPrint(alloc, "{s}/responses", .{base});
+    } else null;
+    defer if (proxy_endpoint) |value| alloc.free(value);
+    const request_endpoint = proxy_endpoint orelse if (io_mod.getenv(e2e_endpoint_env)) |override| endpoint: {
         if (!gateway_client.isLoopbackHttpUrl(override)) {
             return stream_provider.failResult(error.InvalidE2EOpenAICodexEndpoint);
         }
@@ -568,10 +579,12 @@ test "OpenAI Codex request uses Responses input and converts AI SDK tool schemas
         .tools = .{ .additional_functions = &.{read_file_schema} },
         .tool_choice = .auto,
         .provider_options = .{ .reasoning = types.ReasoningEffort.literal("high"), .fast = true },
+        .session_id = "stable-thread-id",
     });
     defer std.testing.allocator.free(body);

     try std.testing.expect(std.mem.find(u8, body, "\"model\":\"gpt-5.4\"") != null);
+    try std.testing.expect(std.mem.find(u8, body, "\"prompt_cache_key\":\"stable-thread-id\"") != null);
     try std.testing.expect(std.mem.find(u8, body, "\"instructions\":\"Be concise.\"") != null);
     try std.testing.expect(std.mem.find(u8, body, "\"type\":\"function_call_output\"") != null);
     try std.testing.expect(std.mem.find(u8, body, "\"encrypted_content\":\"opaque\"") != null);
diff --git a/src/gateway/openai_codex_models.zig b/src/gateway/openai_codex_models.zig
index 8eec6bf..7f88b02 100644
--- a/src/gateway/openai_codex_models.zig
+++ b/src/gateway/openai_codex_models.zig
@@ -218,7 +218,12 @@ const FetchOperation = struct {
 };

 fn modelsUrl(alloc: std.mem.Allocator, version: ?versions.Version) ![]u8 {
-    const base = io_mod.getenv(e2e_models_endpoint_env) orelse default_models_endpoint;
+    const proxy_endpoint: ?[]u8 = if (io_mod.getenv("FX_T3_CODEX_PROXY_URL")) |proxy| endpoint: {
+        if (!gateway_client.isLoopbackHttpUrl(proxy)) return error.InvalidCodexProxy;
+        break :endpoint try std.fmt.allocPrint(alloc, "{s}/models", .{proxy});
+    } else null;
+    defer if (proxy_endpoint) |value| alloc.free(value);
+    const base = proxy_endpoint orelse io_mod.getenv(e2e_models_endpoint_env) orelse default_models_endpoint;
     if (io_mod.getenv(e2e_models_endpoint_env) != null and !gateway_client.isLoopbackHttpUrl(base)) {
         return error.InvalidE2ECodexModelsEndpoint;
     }
diff --git a/src/main.zig b/src/main.zig
index 12feec1..afd8ec4 100644
--- a/src/main.zig
+++ b/src/main.zig
@@ -3788,7 +3788,11 @@ fn emptyEntryConfig(auth_mode: credentials.AuthMode) app_entry_runtime.Config {
 }

 fn runAcpServer(_: ?*anyopaque, alloc: Allocator, cfg: acp_runner.Config) anyerror!void {
-    return acp_server.run(alloc, cfg);
+    var embedded = cfg;
+    embedded.allow_host_tools = true;
+    embedded.allow_native_tools = false;
+    embedded.allow_acp_mcp = false;
+    return acp_server.run(alloc, embedded);
 }

 fn handleSigWinchNative(_: std.posix.SIG) callconv(.c) void {
